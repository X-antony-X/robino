function RobinoIcon({ size = 16 }) {
  return (
    <img
      src={robino.png}
      alt="Robino"
      style={{ width: size, height: size }}
      className="object-contain"
    />
  );
}

export default RobinoIcon